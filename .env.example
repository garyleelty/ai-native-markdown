# ═══════════════════════════════════════════════════════════
# AI Markdown Editor - 环境变量配置模板
# 复制此文件为 .env.local 并填入你的配置
# ═══════════════════════════════════════════════════════════

# ── Ollama 本地模型配置 ──
# 安装: https://ollama.com
# 启动: ollama serve
# 拉取模型: ollama pull qwen2.5:7b

# Ollama 服务地址（一般不需要修改）
VITE_OLLAMA_BASE_URL=http://localhost:11434

# 默认 Ollama 模型
VITE_OLLAMA_MODEL=qwen2.5:7b

# ── OpenAI 兼容 API 配置 ──
# 支持: OpenAI / DeepSeek / 通义千问 / LM Studio / Ollama OpenAI 接口 等

# API Key（必填，从对应平台获取）
VITE_OPENAI_API_KEY=sk-your-api-key-here

# API 地址（按需修改）
# OpenAI 官方: https://api.openai.com/v1
# DeepSeek: https://api.deepseek.com/v1
# 通义千问: https://dashscope.aliyuncs.com/compatible-mode/v1
# LM Studio: http://localhost:1234/v1
VITE_OPENAI_BASE_URL=https://api.openai.com/v1

# 模型名称
VITE_OPENAI_MODEL=gpt-4o-mini

# ── 网络配置说明 ──
# 
# 开发模式下，Vite 已配置代理：
#   /api/ai/ollama/* → http://localhost:11434/*
#   /api/ai/openai/* → https://api.openai.com/v1/*
#
# 如果你使用的是其他 OpenAI 兼容服务（如 DeepSeek），有两种方式：
#
# 方式1（推荐）: 修改 vite.config.ts 中的 proxy target
#   '/api/ai/openai': {
#     target: 'https://api.deepseek.com/v1',
#     ...
#   }
#
# 方式2: 在 AI CONFIG 面板中直填 baseURL（需目标服务支持 CORS）
#   如 https://api.deepseek.com/v1
#
# ── Ollama 常见问题 ──
#
# Q: 连接 Ollama 失败？
# A: 1. 确认已安装 Ollama: https://ollama.com
#    2. 运行 ollama serve 启动服务
#    3. 确认服务运行在 11434 端口
#    4. 如端口不同，修改 vite.config.ts 中 Ollama proxy 的 target
#
# Q: 模型未找到？
# A: 先拉取模型: ollama pull qwen2.5:7b
#    查看已安装: ollama list
#    推荐模型: qwen2.5:7b, llama3.1:8b, mistral:7b, deepseek-coder:6.7b
#
# ── OpenAI 常见问题 ──
#
# Q: CORS 错误？
# A: 不要直接填 https://api.openai.com/v1，使用 /api/ai/openai 代理路径
#    或修改 vite.config.ts 中 proxy 的 target
#
# Q: 401 Unauthorized？
# A: 检查 API Key 是否正确，是否已过期
#
# Q: 429 Too Many Requests？
# A: API 调用频率超限，请稍后重试或升级套餐
